export module howuku {
  /**
   * Implement Howuku Analytics into your Progressive Web Application.
   * @param id This is the ID which tells Howuku which site settings it should load and where it should save the data collected.
   */
  export function initialize(id: string): void;
}